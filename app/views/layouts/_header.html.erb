<nav class="navbar navbar-default" role="navigation">
	<div class="container-fluid">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
		        <span class="sr-only">Toggle navigation</span>
		        <span class="icon-bar"></span>
		        <span class="icon-bar"></span>
		        <span class="icon-bar"></span>
	      	</button>
		<%= link_to "Sample App", root_path, id: "logo", class: "navbar-brand" %>
		</div>

		<div class="collapse navbar-collapse">
			<ul class="nav navbar-nav navbar-right">
				<li class="<%= 'active' if current_page?(root_path) %>"> 
					<%= link_to root_path do %>
						<span class="glyphicon glyphicon-home"></span> Home
					<% end %>
				</li>
				<li class="<%= 'active' if current_page?(help_path) %>"> 
					<%= link_to help_path do %>
						<span class="glyphicon glyphicon-book"></span> Help
					<% end %>
				</li>
				<% if signed_in? %>
					<li class="<%= 'active' if params[:controller] == 'users'%>"> 
						<%= link_to users_path do %>
							<span class="glyphicon glyphicon-user"></span> Users
						<% end %>
					</li>
					<li id="fat-menu" class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown">
							<span class="glyphicon glyphicon-cog"></span> Account <b class="caret"></b>
						</a>
						<ul class="dropdown-menu">
							<li> <%= link_to "Profile", current_user %> </li>
							<li> <%= link_to "Settings", edit_user_path(current_user) %> </li>
							<li class="divider"></li>
							<li> <%= link_to "Sign out", logout_path, method: "delete" %> </li>
						</ul>
					</li>
				<% else %>
					<li class="<%= 'active' if current_page?(login_path)%>"> <%= link_to login_path do %><span class="glyphicon glyphicon-lock"></span> Sign In <% end %></li>
				<% end %>
			</ul>
		</div>
	</div>
</nav>